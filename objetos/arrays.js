var stark = ["Ned", "Jon", "Rob"];

console.log(stark[0]);

for (let index = 0; index < stark.length; index++) {
    console.log(stark[index]);
}

stark.forEach(element => {
    console.log(element);
});

var grupos = [
    ["Ned", "Jon", "Rob"],
    ["João", "José", "Juca"]
    ["Maria", "Mariana", "Matilde"]  
];